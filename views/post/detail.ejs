<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <a href="/">Home</a>

    Post Detail

    <div>
      <% if (currentUser && (currentUser.id == post.user.id || currentUser.status === "Member+" || currentUser.status === "Admin")) { %>
        <li>By: <%= post.user.full_name %></li>
      <% } else { %>
        <li>Anonymous</li>
      <% } %>
      <li>Posted on: <%= post.date_formatted %></li>
      <li><%- post.post %></li>
    </div>

    <%# Displays delete button only if post belongs to the current user or if the current user is an Admin. %>
    <% if (currentUser) { %>
      <% if (currentUser.id == post.user.id || currentUser.status === "Admin") { %>
        <a href="<%= post.url %>/delete">Delete Post</a>
      <% } %>
    <% } %>
  </body>
</html>
