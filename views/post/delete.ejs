<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/reset.css" />
    <link rel="stylesheet" href="/stylesheets/delete-post.css">
    <link rel="stylesheet" href="/stylesheets/partials/post.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include("../partials/navbar"); %>

    <div class="prompt-container">
      <p class="delete-prompt">Are you sure you want to delete this post?</p>
    </div>

    <%# Displays delete confirmation form only if post belongs to the current user or if the current user is an Admin. %>
    <% if (currentUser) { %>
      <% if (currentUser.id == post.user || currentUser.status === "Admin") { %>
      <form method="POST" action="" class="form">
        <button type="submit">Permanantly Delete</button>
      </form>
      <% } %>
    <% } %>

    <div class="posts-container">
      <% if (post) { %>
          <%- include("../partials/post", {post: post}); %>
      <% } else { %>
        <p>No posts</p>
      <% } %>
    </div>
  </body>
</html>
