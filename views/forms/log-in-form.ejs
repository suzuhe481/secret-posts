<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/reset.css" />
    <link rel="stylesheet" href="/stylesheets/forms/log-in-form.css">
  </head>
  <body>
    <%- include("../partials/navbar"); %>
    
    <form action="/log-in" method="POST" class="form">
      <div class="form-title">
        <p>Please Log In</p>
      </div>

      <div class="form-group">
        <label for="email">Email</label>
        <input type="text" name="email" />
      </div>

      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" name="password" />
      </div>

      <div class="form-group">
        <button type="submit">Log In</button>
      </div>
    </form>

    <% if (typeof messages != "undefined") { %>
      <p> Error: <%= messages[0] %> </p>
    <% } %>

    <div class="account-create-container">
      <p>Need an account?</p>
      <a href="/user/create">Create an account here</a>
    </div>

    <% if (locals.errors) { %>
    <div>
      <% for( let i = 0; i < errors.length; i++ ) { %>
      <li><%= errors[i].msg %></li>
      <% } %>
    </div>
    <% } %> 
    <% if (locals.customErrors) { %>
    <div>
      <% for( let i = 0; i < customErrors.length; i++ ) { %>
      <li><%= customErrors[i] %></li>
      <% } %>
    </div>
    <% } %>
  </body>
</html>
